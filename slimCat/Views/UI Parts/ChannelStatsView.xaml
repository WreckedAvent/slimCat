<Popup x:Class="Slimcat.Views.ChannelStatsView"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:vm="clr-namespace:Slimcat.ViewModels"
       mc:Ignorable="d"
       
       PlacementTarget="{Binding ElementName=PopupAnchor}"
       Placement="Center"
       StaysOpen="False"
       IsOpen="{Binding Path=ChannelStats.IsViewingStats}"
       
       d:DataContext="{d:DesignInstance vm:GeneralChannelViewModel}">
    <Grid>

    </Grid>
</Popup>