<UserControl x:Class="Views.CharacterSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System">
    <UserControl.Resources>
        <sys:OppositeBoolConverter x:Key="BoolConverter" />
    </UserControl.Resources>

    <Grid IsEnabled="{Binding Path=IsConnecting, Converter={StaticResource BoolConverter}}"
          TextBlock.FontSize="20"
          TextBlock.Foreground="{StaticResource ForegroundBrush}"
          Margin="20"
          VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding RelayMessage, Mode=OneWay}"
                   HorizontalAlignment="Center"
                   Margin="20" />
        <ScrollViewer
            Grid.Row="1"
            MinWidth="200">
            <ListBox ItemsSource="{Binding Characters}"
                     SelectedItem="{Binding Path=SelectedCharacter, Mode=TwoWay}" />
        </ScrollViewer>

        <Button Content="Login"
                Margin="20"
                Grid.Row="2"
                HorizontalAlignment="Center"
                Command="{Binding SelectCharacterCommand}" />
    </Grid>
</UserControl>